День 2 — Тест-дизайн

Это очень важная часть собеседования.

Тебе нужно освоить:

Эквивалентное разбиение

Например:

Поле принимает возраст от 18 до 60.

Не надо тестировать:

18, 19, 20, 21, 22...60.

Можно разделить:

<18        → invalid
18–60      → valid
>60        → invalid
Граничные значения

Для диапазона:

18–60

проверяем:

17
18
19

59
60
61

Это одна из самых популярных техник на собеседованиях.

Таблица решений

Например, интернет-магазин:

Есть товар?
Есть деньги?
Пользователь авторизован?

В зависимости от комбинации условий система должна разрешать или запрещать покупку.

Попарное тестирование

Понимать идею достаточно на начальном уровне.

Когда параметров много, мы проверяем комбинации параметров попарно, вместо полного перебора всех вариантов.

State Transition

Например, аккаунт:

Active
   ↓
Wrong password
   ↓
Wrong password
   ↓
Locked

Тебе нужно понимать, что поведение системы может зависеть от предыдущего состояния.

Практическое задание

Я дам тебе задачу:

Поле «Возраст» принимает значения от 18 до 60 включительно.

Составь тестовые значения.

Правильный подход:

17
18
19
59
60
61

День 3 — Web Testing + DevTools

Здесь уже начинается то, что тебе очень пригодится на первой работе.

Тебе нужно понимать:

Browser
   ↓
Frontend
   ↓
HTTP request
   ↓
Backend
   ↓
Database

Когда пользователь нажимает кнопку, происходит намного больше, чем просто изменение страницы.

DevTools

Тебе нужно уверенно знать:

Elements

Уметь:

найти элемент
посмотреть HTML
посмотреть CSS
изменить CSS
найти class/id
посмотреть attributes
Console

Понимать:

errors
warnings
JavaScript errors

Например:

Uncaught TypeError
404
CORS error
Network

Это один из самых важных разделов.

Ты должен понимать:

Request
Response
Headers
Payload
Query Parameters
Status Code
Response Body

И HTTP-коды:

200
201
204

400
401
403
404
409
422

500
502
503

Например:

401 — сервер не получил корректную аутентификацию.

403 — сервер понял запрос, но доступ запрещён.

404 — ресурс не найден.

500 — внутренняя ошибка сервера.

Практика

Открой любой сайт.

DevTools → Network

Нажми:

Login
Search
Add to cart
Submit

И посмотри:

какой запрос отправился
куда
какой метод
какие headers
какой status code
что ушло в request
что пришло в response

После этого ты уже начнёшь понимать API.
add line
